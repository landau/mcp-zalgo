{
  "name": "mcp-zalgo",
  "private": true,
  "version": "1.0.0",
  "description": "MCP server that converts text to zalgo format",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "build": "tsc && cp src/wrapper-template.ts dist/wrapper-template.ts",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "prepare": "npm run build"
  },
  "keywords": ["mcp", "zalgo", "text"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^0.5.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  }
}

